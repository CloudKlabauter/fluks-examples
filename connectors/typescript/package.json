{
  "name": "ck.kalypso.examples.connectors.typescript",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/index.js",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "build": "yarn rimraf dist/* && tsc --build",
    "build:watch": "tsc --build --watch"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/uuid": "^9.0.0",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  }
}
